#!/bin/bash

for pkg in $(rpm -qa --qf '%{NAME}\n')
do
  nice -n 19 ionice -c 3 rpm --verify $pkg | grep -v ' /etc/' | grep -v ' /root/' | egrep '^..5' | while read line; do echo -e "${pkg}\t${line}"; done
done
